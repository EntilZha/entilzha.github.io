---
import Layout from "./Layout.astro";

interface Props {
  title: string;
  description?: string;
  backHref?: string;
  backLabel?: string;
}

const { title, description, backHref, backLabel } = Astro.props;
---

<Layout title={`${title} · Pedro Rodriguez`} description={description}>
  <article
    class="prose prose-gray max-w-none prose-a:text-cyan-600 prose-headings:font-semibold"
  >
    {
      backHref && (
        <a href={backHref} class="not-prose text-sm text-cyan-600 hover:underline">
          &larr; {backLabel}
        </a>
      )
    }
    <h1>{title}</h1>
    <slot />
  </article>
  <slot name="after" />
</Layout>
